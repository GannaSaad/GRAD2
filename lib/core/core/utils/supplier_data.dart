class SupplierRequestsData {
  // Mock data removed to ensure sync with Firebase only.
  static final List<Map<String, String>> allRequests = [];
  
  static void updateStatus(String id, String newStatus) {
    // This is now handled directly via Firestore update use cases.
  }
}
